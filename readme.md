# Build the ActiveMQ docker image
1. build using the [build.sh](image/build.sh) script
2. run using [run.sh](image/run.sh)
3. open the [broker console](http://127.0.0.1:8161/) and login with admin:admin
